import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  footer: {
    height: 32,
    backgroundColor: '#229911',
  },
  footerTxt: {
    lineHeight: 32,
    textAlign: 'center',
  }
});